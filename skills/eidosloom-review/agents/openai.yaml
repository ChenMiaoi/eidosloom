interface:
  display_name: "Eidosloom Review"
  short_description: "Review with GPT web at chosen rigor"
  default_prompt: "Use $eidosloom-review at deep level to review the current implementation and return a gate decision with required fixes."
